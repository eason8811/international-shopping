<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shopping.international.infrastructure.dao.customerservice.AfterSalesReshipShipmentMapper">

    <insert id="batchInsert">
        INSERT INTO aftersales_reship_shipment (reship_id, shipment_id)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.reshipId}, #{item.shipmentId})
        </foreach>
    </insert>

    <select id="listByShipmentIds" resultType="shopping.international.infrastructure.dao.customerservice.po.AfterSalesReshipShipmentPO">
        SELECT rs.reship_id,
               rs.shipment_id,
               rs.created_at
        FROM aftersales_reship_shipment rs
        WHERE rs.shipment_id IN
        <foreach collection="shipmentIds" item="shipmentId" open="(" close=")" separator=",">
            #{shipmentId}
        </foreach>
        ORDER BY rs.shipment_id ASC
    </select>
</mapper>
